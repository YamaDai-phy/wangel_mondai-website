{
  "name": "pdf-upload-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js && node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply wangel-papers --local",
    "db:migrate:remote": "wrangler d1 migrations apply wangel-papers --remote",
    "types": "wrangler types",
    "migrate:library": "node scripts/migrate-static-library.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
